/* eslint-disable react/prop-types */


const GraphicDesign = ({job}) => {
    const {title}=job;
    return (
        <div>
            {title}
        </div>
    );
};

export default GraphicDesign;